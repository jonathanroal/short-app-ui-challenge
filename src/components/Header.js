import Grid from '@mui/material/Grid';

export default function UrlInput() {
  return (
    <Box
      sx={{
        marginTop: '30px',
        width: '80%',
        height: 300,
        backgroundColor: 'white',
        borderRadius: '10px',
        boxShadow: 3,
      }}
    >

    </Box>
  )
}